# Salin ke .env (jangan commit): cp .env.example .env
# Neon: dashboard → Connect → pooled connection string
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.ap-southeast-1.aws.neon.tech/dbname?sslmode=require
PORT=8081
# URL frontend untuk CORS. Koma-pisahkan bila lebih dari satu.
WEB_ORIGIN=http://localhost:5173
NODE_ENV=development
